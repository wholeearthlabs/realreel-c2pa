# Changesets

This repo uses [Changesets](https://github.com/changesets/changesets) to version
and publish its npm packages — `@realreel/c2pa-trust-core` and
`@realreel/photo-attest`. For a releasable change to either, run `npx changeset`
and commit the generated file. See **Releasing** in [`AGENTS.md`](../AGENTS.md).
