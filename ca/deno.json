{
  "//": "Deno project config for edge function code under ca/.",
  "//1": "Lives at ca/ rather than repo root so its node_modules/.deno cache",
  "//2": "doesn't intermix with the React Native project's yarn-managed node_modules.",
  "//3": "no-import-prefix wants the inline std/esm.sh/npm: pins moved into the",
  "//4": "import map. These deploy from source through a pipeline outside this repo",
  "//5": "(RELEASING.md), so we can't verify that deploy honors the map — leave them.",
  "//6": "fmt skips markdown: it's hand-wrapped here and deno fmt reflows it.",
  "nodeModulesDir": "auto",
  "lint": {
    "rules": {
      "exclude": ["no-import-prefix"]
    }
  },
  "fmt": {
    "exclude": ["**/*.md"]
  },
  "imports": {
    "pkijs": "npm:pkijs@3.2.4",
    "asn1js": "npm:asn1js@3.0.5",
    "cbor-x": "npm:cbor-x@1.5.9"
  }
}
