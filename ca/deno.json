{
  "//": "Deno project config for edge function code under ca/.",
  "//1": "Lives at ca/ rather than repo root so its node_modules/.deno cache",
  "//2": "doesn't intermix with the React Native project's yarn-managed node_modules.",
  "//3": "Pins live in _shared/import_map.json rather than inline here (the sibling",
  "//4": "services' style): each function dir carries a deno.json pointing Supabase's",
  "//5": "deploy bundling at that map, and it is vendored into the closed app with",
  "//6": "the rest of ca/ — one pin set for OSS tests, app tests, and deploys.",
  "//7": "fmt skips markdown: it's hand-wrapped here and deno fmt reflows it.",
  "nodeModulesDir": "auto",
  "importMap": "./_shared/import_map.json",
  "fmt": {
    "exclude": ["**/*.md"]
  }
}
